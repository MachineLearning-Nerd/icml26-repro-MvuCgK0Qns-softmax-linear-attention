# Claim 1 evaluation

Verdict: **FALSIFIED**, for the literal published Proposition 3.1.

The valid Gaussian witness gives exact `LHS=1` and `RHS=0` at `L=1`, independent
of the unknown positive constants. This is a quantified counterexample, not a
finite-scale approximation.

Interpretation risk: an unstated `L>=2` or sufficiently-large-`L` restriction
would repair this specific defect. The paper does not state such a restriction
in Proposition 3.1, so the certificate does not silently add it.
