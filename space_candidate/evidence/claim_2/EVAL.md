# Claim 2 evaluation

Verdict: **FALSIFIED**, for the literal published Proposition 3.4.

The standard-Gaussian witness satisfies the complete nine-case moment
assumption. At `L=1`, the exact `V`- and `U`-gradient error left sides both
equal `1`, while their right sides both equal `0` independently of the
unknown positive constants.

Interpretation risk: an unstated `L>=2` or sufficiently-large-`L` restriction
would repair this specific defect. Proposition 3.4 does not state one, so the
certificate does not silently modify its quantifiers.
